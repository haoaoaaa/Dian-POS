# 711 便利店 POS 系统 — 学习日志
学习日志

项目

711便利店POS系统，C语言写的，命令行交互。

踩过的坑

环境
第一天连终端都不会开，nano pos.c 少打了空格，结果建了个隐藏文件，以为代码没了吓哭了
代码被清空过一次，幸好Git里有存档 从此养成了随手commit的习惯

语法
for 循环里总爱写逗号，被编译器骂了无数次
变量名拼错是常态：car_count、littletotale、inout
括号经常不配对，满屏expected '}'。

逻辑
扫码加减购卡了两天。搞不清 items[i].name 和 cart[j].item.name 的区别，后来才明白：一个是货架上的商品，一个是购物车里的记录。
减购减到0要用“覆盖法”删除：把最后一个元素搬过来，总数减1。

文件读写
 FILE *fp 一开始完全不懂，后来用“寄快递”理解：fopen领单号，fprintf塞东西，fclose发货。
 fopen 用 "a" 模式，文件不存在会自动创建，不用手动新建。
 fgets 会读进回车符，用 strcspn 清掉，不然 sscanf 解析出错。

管理员模式
写完忘了写 back，卡死在 admin> 里出不来。
进管理员模式时 fgets 被缓冲区残留回车坑了，加 getchar() 解决。
库存扣减放在扫码阶段而不是结账阶段，防止多个顾客同时抢最后一件造成超卖。

感受

一周前连终端都打不开，现在能自己写一个带文件存储、管理员系统、库存管理的POS系统。虽然代码不算优雅，但每一行都是自己查资料、报错、改错搞出来的
感谢一直指导我的deepseek老师
